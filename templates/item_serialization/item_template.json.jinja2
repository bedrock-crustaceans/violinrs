{
  "format_version": "{{format_version}}",
  "minecraft:item": {
    "description": {
      "identifier": "{{id}}"
    },
    "components": {
    {% for component in components %}
    {{ component }}
    {% endfor %}
  }
  }
}
